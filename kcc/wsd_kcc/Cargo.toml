[package]
name = "wsd_kcc"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib", "rlib"]

[dependencies]
kcc = { path = ".." }
once_cell = "1.18"
uuid = { version = "1.3", features = ["v4"] }
